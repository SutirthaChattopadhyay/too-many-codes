#include<iostream>
 using namespace std;
    int main()
    {   
	    int a,b,c; 
    	cout<<"enter two no-->";
    	  cin>>a;
/*    	  cout<<"The sum of the no is-->"<<a+b;
		  cout<<"The subtraction of the no is-->"<<a-b;
		  cout<<"The multiplication of the no is-->"<<a*b;
		  cout<<"The division of the no is-->"<<a/b;
		  cout<<"The modulus of the no is-->"<<a%b;*/ 
		    if(a%2==0)
		       cout<<"even";
		    else 
			   cout<<"odd";   
    	 return 0;
	}
 	
